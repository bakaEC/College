#include <stdio.h>
#include <stdlib.h>
int main(int argc, char *argv[]) {
	int a=0,i=0,sum=0;
	while(i<0)
		{printf("input:");
		 scanf("%d",a);
		 if (a=0);
		  continue;
		 sum += a;
		 i++;
		 } 
		 printf("sum=%d",sum);
	return 0;
}
